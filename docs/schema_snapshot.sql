-- Schema snapshot — auto-generated by scripts/refresh_schema.py
-- DO NOT EDIT BY HAND.
-- Regenerate: cd apps/api && DATABASE_URL='...' python scripts/refresh_schema.py

-- ===== assistant_action_catalog =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   action_key                               text NOT NULL
--   module                                   text NOT NULL
--   description                              text
--   access_type                              text NOT NULL
--   required_permission                      text
--   default_autonomy                         text NOT NULL DEFAULT 'confirm'::text
--   reversible                               boolean NOT NULL DEFAULT false
--   render_target                            text NOT NULL DEFAULT 'auto'::text
--   is_active                                boolean NOT NULL DEFAULT true
--   registered_at                            timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE assistant_action_catalog_org_id_action_key_key: (org_id, action_key)
--   PRIMARY KEY assistant_action_catalog_pkey: (id)

-- ===== assistant_activities =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   user_id                                  uuid NOT NULL
--   action_key                               text NOT NULL
--   title                                    text NOT NULL
--   status                                   text NOT NULL DEFAULT 'awaiting_review'::text
--   rationale                                text
--   payload                                  jsonb
--   result                                   jsonb
--   reversible                               boolean NOT NULL DEFAULT false
--   undo_token                               jsonb
--   undone_at                                timestamp with time zone
--   related_type                             text
--   related_id                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY assistant_activities_pkey: (id)

-- ===== assistant_autonomy_prefs =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   user_id                                  uuid NOT NULL
--   action_key                               text NOT NULL
--   autonomy                                 text NOT NULL
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY assistant_autonomy_prefs_pkey: (id)
--   UNIQUE assistant_autonomy_prefs_user_id_action_key_key: (user_id, action_key)

-- ===== assistant_conversations =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   user_id                                  uuid NOT NULL
--   title                                    text
--   messages                                 jsonb NOT NULL DEFAULT '[]'::jsonb
--   context_ref                              jsonb
--   status                                   text NOT NULL DEFAULT 'active'::text
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY assistant_conversations_pkey: (id)

-- ===== audit_log =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   user_id                                  uuid
--   action                                   text NOT NULL
--   resource_type                            text
--   resource_id                              uuid
--   payload                                  jsonb
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY audit_log_pkey: (id)

-- ===== chart_of_accounts =====
--   id                                       uuid NOT NULL DEFAULT gen_random_uuid()
--   org_id                                   uuid NOT NULL
--   code                                     text NOT NULL
--   name                                     text NOT NULL
--   account_type                             text NOT NULL
--   normal_balance                           character NOT NULL
--   tax_character_code                       text
--   parent_code                              text
--   is_capital_account                       boolean NOT NULL DEFAULT false
--   is_active                                boolean NOT NULL DEFAULT true
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   PRIMARY KEY chart_of_accounts_pkey: (id)

-- ===== compliance_override_requests =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   deal_id                                  uuid NOT NULL
--   user_id                                  uuid NOT NULL
--   entity_id                                uuid
--   request_notes                            text
--   status                                   text NOT NULL DEFAULT 'pending'::text
--   reviewed_by                              uuid
--   review_notes                             text
--   reviewed_at                              timestamp with time zone
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE compliance_override_requests_deal_id_user_id_key: (deal_id, user_id)
--   PRIMARY KEY compliance_override_requests_pkey: (id)

-- ===== compliance_records =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   kyc_status                               USER-DEFINED NOT NULL DEFAULT 'not_started'::kyc_status
--   kyc_verified_date                        date
--   kyc_verified_by                          uuid
--   ofac_screen_status                       USER-DEFINED NOT NULL DEFAULT 'not_screened'::ofac_status
--   ofac_screen_date                         timestamp with time zone
--   aml_risk_rating                          USER-DEFINED NOT NULL DEFAULT 'low'::aml_risk_rating
--   accreditation_status                     USER-DEFINED NOT NULL DEFAULT 'not_verified'::accreditation_status
--   accreditation_basis                      text
--   accreditation_verified_date              date
--   next_reverification_due                  date
--   pep_status                               boolean NOT NULL DEFAULT false
--   pep_details                              text
--   notes                                    text
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE compliance_records_entity_id_key: (entity_id)
--   PRIMARY KEY compliance_records_pkey: (id)

-- ===== config =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   config_key                               text NOT NULL
--   config_value                             text NOT NULL
--   value_type                               text NOT NULL DEFAULT 'string'::text
--   category                                 text NOT NULL DEFAULT 'general'::text
--   display_order                            integer NOT NULL DEFAULT 0
--   is_active                                boolean NOT NULL DEFAULT true
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE config_org_id_config_key_key: (org_id, config_key)
--   PRIMARY KEY config_pkey: (id)

-- ===== dashboard_briefs =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   user_id                                  uuid NOT NULL
--   greeting                                 text
--   narration                                text
--   blocks                                   jsonb NOT NULL DEFAULT '[]'::jsonb
--   generated_at                             timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY dashboard_briefs_pkey: (id)
--   UNIQUE dashboard_briefs_user_id_key: (user_id)

-- ===== deal_ai_summaries =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   deal_id                                  uuid NOT NULL
--   summary_text                             text NOT NULL
--   key_strengths                            ARRAY
--   key_risks                                ARRAY
--   market_context                           text
--   input_sources                            jsonb
--   model_used                               text
--   generated_at                             timestamp with time zone NOT NULL DEFAULT now()
--   generated_by                             uuid
--   is_current                               boolean NOT NULL DEFAULT true
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY deal_ai_summaries_pkey: (id)

-- ===== deal_documents =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   deal_id                                  uuid NOT NULL
--   file_name                                text NOT NULL
--   file_type                                text NOT NULL
--   file_size_bytes                          bigint
--   r2_key                                   text NOT NULL
--   r2_bucket                                text NOT NULL
--   document_type                            text NOT NULL DEFAULT 'general'::text
--   processing_status                        USER-DEFINED NOT NULL DEFAULT 'pending'::deal_document_status
--   extracted_data                           jsonb
--   extraction_model                         text
--   extraction_date                          timestamp with time zone
--   uploaded_by                              uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   status                                   text NOT NULL DEFAULT 'pending'::text
--   reviewed_by                              uuid
--   review_notes                             text
--   reviewed_at                              timestamp with time zone
--   visible_to_members                       boolean NOT NULL DEFAULT false
--   PRIMARY KEY deal_documents_pkey: (id)

-- ===== deal_interest =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   deal_id                                  uuid NOT NULL
--   user_id                                  uuid NOT NULL
--   entity_id                                uuid
--   status                                   text NOT NULL DEFAULT 'indicated'::text
--   amount_interest                          numeric
--   notes                                    text
--   compliance_override                      boolean NOT NULL DEFAULT false
--   override_by                              uuid
--   override_notes                           text
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   investment_stage                         text DEFAULT 'interest_indicated'::text
--   UNIQUE deal_interest_deal_id_user_id_key: (deal_id, user_id)
--   PRIMARY KEY deal_interest_pkey: (id)

-- ===== deal_scores =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   deal_id                                  uuid NOT NULL
--   dimension                                text NOT NULL
--   score                                    numeric
--   weight                                   numeric NOT NULL DEFAULT 0.1667
--   notes                                    text
--   scored_by                                uuid
--   scored_by_ai                             boolean NOT NULL DEFAULT false
--   ai_model                                 text
--   ai_confidence                            numeric
--   override_of                              uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE deal_scores_deal_id_dimension_key: (deal_id, dimension)
--   PRIMARY KEY deal_scores_pkey: (id)

-- ===== deal_votes =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   deal_id                                  uuid NOT NULL
--   user_id                                  uuid NOT NULL
--   vote                                     smallint NOT NULL
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE deal_votes_deal_id_user_id_key: (deal_id, user_id)
--   PRIMARY KEY deal_votes_pkey: (id)

-- ===== deals =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   name                                     text NOT NULL
--   slug                                     text
--   description                              text
--   deal_status                              USER-DEFINED NOT NULL DEFAULT 'draft'::deal_status
--   asset_super_class                        text
--   asset_class                              text
--   asset_sub_category                       text
--   sponsor_entity_id                        uuid
--   sponsor_name_override                    text
--   target_raise                             numeric
--   minimum_investment                       numeric
--   expected_return_pct                      numeric
--   term_months                              integer
--   deal_date                                date
--   close_date                               date
--   location                                 text
--   highlights                               ARRAY
--   tags                                     ARRAY
--   is_featured                              boolean NOT NULL DEFAULT false
--   submitted_by                             uuid
--   reviewed_by                              uuid
--   published_at                             timestamp with time zone
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   deal_stage                               text DEFAULT 'sourced'::text
--   PRIMARY KEY deals_pkey: (id)
--   UNIQUE deals_slug_key: (slug)

-- ===== entities =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_type                              USER-DEFINED NOT NULL
--   display_name                             text NOT NULL
--   legal_name                               text
--   tax_id                                   text
--   date_of_birth                            date
--   country_of_formation                     text
--   notes                                    text
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   sub_type                                 text
--   status                                   text NOT NULL DEFAULT 'prospect'::text
--   lead_source                              text
--   relationship_manager_id                  uuid
--   tags                                     ARRAY DEFAULT '{}'::text[]
--   linkedin_url                             text
--   primary_email                            text
--   primary_phone                            text
--   profile_mode                             text NOT NULL DEFAULT 'foundation'::text
--   name_prefix                              text
--   first_name                               text
--   middle_name                              text
--   surname                                  text
--   name_suffix                              text
--   legal_name_overridden                    boolean NOT NULL DEFAULT false
--   inception_date                           date
--   end_date                                 date
--   is_active                                boolean NOT NULL DEFAULT true
--   url                                      text
--   country_code                             text
--   region_code                              text
--   is_incomplete                            boolean NOT NULL DEFAULT false
--   created_via                              text
--   PRIMARY KEY entities_pkey: (id)

-- ===== entity_addresses =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   address_type                             USER-DEFINED NOT NULL DEFAULT 'primary_residence'::address_type
--   street1                                  text NOT NULL
--   street2                                  text
--   city                                     text NOT NULL
--   state                                    text
--   postal_code                              text
--   country                                  text NOT NULL DEFAULT 'US'::text
--   is_verified                              boolean NOT NULL DEFAULT false
--   is_primary                               boolean NOT NULL DEFAULT false
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   phone                                    text
--   country_code                             text
--   region_code                              text
--   is_seasonal                              boolean NOT NULL DEFAULT false
--   season_from_month                        integer
--   season_to_month                          integer
--   PRIMARY KEY entity_addresses_pkey: (id)

-- ===== entity_attributes =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   attribute_key                            text NOT NULL
--   attribute_value                          text
--   value_type                               text NOT NULL DEFAULT 'string'::text
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY entity_attributes_pkey: (id)

-- ===== entity_briefs =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   brief_text                               text NOT NULL
--   key_themes                               ARRAY
--   risk_profile                             text
--   decision_style                           text
--   relationship_notes                       text
--   suitability_notes                        text
--   input_sources                            jsonb
--   model_used                               text
--   is_current                               boolean NOT NULL DEFAULT true
--   generated_by                             uuid
--   generated_at                             timestamp with time zone NOT NULL DEFAULT now()
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY entity_briefs_pkey: (id)

-- ===== entity_document_tags =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   document_id                              uuid NOT NULL
--   tag                                      text NOT NULL
--   is_fixed                                 boolean NOT NULL DEFAULT false
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE entity_document_tags_document_id_tag_key: (document_id, tag)
--   PRIMARY KEY entity_document_tags_pkey: (id)

-- ===== entity_documents =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   title                                    text NOT NULL
--   doc_category                             text
--   storage_key                              text
--   file_name                                text
--   content_type                             text
--   file_size                                bigint
--   version                                  integer NOT NULL DEFAULT 1
--   supersedes_id                            uuid
--   status                                   text NOT NULL DEFAULT 'active'::text
--   uploaded_by                              uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY entity_documents_pkey: (id)

-- ===== entity_employment =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   employee_id                              uuid NOT NULL
--   employer_id                              uuid NOT NULL
--   title                                    text
--   start_date                               date
--   end_date                                 date
--   is_current                               boolean NOT NULL DEFAULT false
--   notes                                    text
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY entity_employment_pkey: (id)

-- ===== entity_group_members =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   group_id                                 uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE entity_group_members_group_id_entity_id_key: (group_id, entity_id)
--   PRIMARY KEY entity_group_members_pkey: (id)

-- ===== entity_groups =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   name                                     text NOT NULL
--   description                              text
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY entity_groups_pkey: (id)

-- ===== entity_holdings =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   taxonomy_key                             text NOT NULL
--   market_value                             numeric NOT NULL
--   currency_code                            text NOT NULL DEFAULT 'USD'::text
--   as_of_date                               date NOT NULL DEFAULT CURRENT_DATE
--   source                                   text NOT NULL DEFAULT 'manual'::text
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY entity_holdings_pkey: (id)

-- ===== entity_notes =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   note_text                                text NOT NULL
--   note_type                                text NOT NULL DEFAULT 'meeting'::text
--   meeting_date                             date
--   extracted_fields                         jsonb
--   extraction_model                         text
--   extraction_status                        text NOT NULL DEFAULT 'pending'::text
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY entity_notes_pkey: (id)

-- ===== entity_ownership =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   parent_id                                uuid NOT NULL
--   child_id                                 uuid NOT NULL
--   ownership_pct                            numeric NOT NULL
--   ownership_type                           text NOT NULL DEFAULT 'equity'::text
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY entity_ownership_pkey: (id)

-- ===== entity_relationships =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   from_entity_id                           uuid NOT NULL
--   to_entity_id                             uuid NOT NULL
--   relationship_type                        text NOT NULL
--   notes                                    text
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   ownership_pct                            numeric
--   change_reason                            text
--   change_source_type                       text
--   change_source_id                         uuid
--   effective_date                           date
--   PRIMARY KEY entity_relationships_pkey: (id)

-- ===== entity_social_profiles =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   platform                                 USER-DEFINED NOT NULL
--   url                                      text NOT NULL
--   is_primary                               boolean NOT NULL DEFAULT false
--   linkedin_import_stub                     boolean NOT NULL DEFAULT false
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE entity_social_profiles_entity_id_platform_key: (entity_id, platform)
--   PRIMARY KEY entity_social_profiles_pkey: (id)

-- ===== entity_tax_ids =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   tax_id_type                              USER-DEFINED NOT NULL DEFAULT 'ssn'::tax_id_type
--   tax_id_country                           text NOT NULL DEFAULT 'US'::text
--   tax_id_encrypted                         text NOT NULL
--   tax_id_last4                             text NOT NULL
--   is_primary                               boolean NOT NULL DEFAULT true
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY entity_tax_ids_pkey: (id)

-- ===== fx_rates =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid
--   base_ccy                                 text NOT NULL DEFAULT 'USD'::text
--   quote_ccy                                text NOT NULL
--   rate                                     numeric NOT NULL
--   as_of_date                               date NOT NULL
--   source                                   text
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE fx_rates_base_ccy_quote_ccy_as_of_date_key: (base_ccy, quote_ccy, as_of_date)
--   PRIMARY KEY fx_rates_pkey: (id)

-- ===== investment_profile_answers =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   question_id                              uuid NOT NULL
--   answer_value                             text
--   answer_json                              jsonb
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE investment_profile_answers_entity_id_question_id_key: (entity_id, question_id)
--   PRIMARY KEY investment_profile_answers_pkey: (id)

-- ===== investment_profile_extractions =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   question_id                              uuid NOT NULL
--   answer_id                                uuid NOT NULL
--   extracted_fields                         jsonb NOT NULL
--   extraction_model                         text
--   confidence                               numeric
--   advisor_reviewed                         boolean NOT NULL DEFAULT false
--   advisor_accepted                         boolean
--   reviewed_by                              uuid
--   reviewed_at                              timestamp with time zone
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY investment_profile_extractions_pkey: (id)

-- ===== investment_profile_questions =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   question_key                             text NOT NULL
--   question_text                            text NOT NULL
--   question_type                            text NOT NULL DEFAULT 'text'::text
--   options                                  jsonb
--   category                                 text NOT NULL DEFAULT 'general'::text
--   is_required                              boolean NOT NULL DEFAULT false
--   display_order                            integer NOT NULL DEFAULT 0
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY investment_profile_questions_pkey: (id)
--   UNIQUE investment_profile_questions_question_key_key: (question_key)

-- ===== investment_stage_history =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   member_investment_id                     uuid NOT NULL
--   from_stage                               text
--   to_stage                                 text NOT NULL
--   changed_by                               uuid
--   notes                                    text
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY investment_stage_history_pkey: (id)

-- ===== journal_entries =====
--   id                                       uuid NOT NULL DEFAULT gen_random_uuid()
--   org_id                                   uuid NOT NULL
--   vehicle_id                               uuid NOT NULL
--   entry_date                               date NOT NULL
--   ledger_basis                             text NOT NULL DEFAULT 'GAAP'::text
--   transaction_type_code                    text
--   memo                                     text
--   source_event_id                          uuid
--   reverses_entry_id                        uuid
--   reversal_reason                          text
--   posted_at                                timestamp with time zone
--   posted_by                                uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   created_by                               uuid
--   PRIMARY KEY journal_entries_pkey: (id)

-- ===== journal_lines =====
--   id                                       uuid NOT NULL DEFAULT gen_random_uuid()
--   entry_id                                 uuid NOT NULL
--   line_no                                  integer NOT NULL
--   account_id                               uuid NOT NULL
--   debit                                    numeric NOT NULL DEFAULT 0
--   credit                                   numeric NOT NULL DEFAULT 0
--   currency_code                            text NOT NULL DEFAULT 'USD'::text
--   dim_member_series_id                     uuid
--   dim_investment_id                        uuid
--   dim_tax_lot_id                           uuid
--   memo                                     text
--   UNIQUE jl_line_unique: (entry_id, line_no)
--   PRIMARY KEY journal_lines_pkey: (id)

-- ===== member_investments =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   deal_id                                  uuid NOT NULL
--   user_id                                  uuid NOT NULL
--   entity_id                                uuid
--   investment_stage                         text NOT NULL DEFAULT 'interest_indicated'::text
--   amount_committed                         numeric
--   amount_funded                            numeric
--   subdoc_sent_at                           timestamp with time zone
--   subdoc_executed_at                       timestamp with time zone
--   funded_at                                timestamp with time zone
--   stage_updated_at                         timestamp with time zone NOT NULL DEFAULT now()
--   stage_updated_by                         uuid
--   notes                                    text
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE member_investments_deal_id_user_id_key: (deal_id, user_id)
--   PRIMARY KEY member_investments_pkey: (id)

-- ===== member_target_allocations =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   user_id                                  uuid
--   entity_id                                uuid NOT NULL
--   taxonomy_key                             text NOT NULL
--   taxonomy_level                           text NOT NULL
--   target_pct                               numeric NOT NULL
--   notes                                    text
--   set_by                                   uuid
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   system_from                              timestamp with time zone NOT NULL DEFAULT now()
--   system_to                                timestamp with time zone
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY member_target_allocations_pkey: (id)

-- ===== member_todos =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   user_id                                  uuid NOT NULL
--   entity_id                                uuid
--   kind                                     text NOT NULL DEFAULT 'actual'::text
--   category                                 text NOT NULL
--   title                                    text NOT NULL
--   detail                                   text
--   action_key                               text
--   action_params                            jsonb
--   due_date                                 date
--   expected_window                          text
--   priority                                 integer NOT NULL DEFAULT 100
--   status                                   text NOT NULL DEFAULT 'open'::text
--   source                                   text
--   related_type                             text
--   related_id                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY member_todos_pkey: (id)

-- ===== notification_delivery_log =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   notification_id                          uuid NOT NULL
--   recipient_id                             uuid NOT NULL
--   channel                                  text NOT NULL
--   status                                   text NOT NULL DEFAULT 'pending'::text
--   attempted_at                             timestamp with time zone
--   delivered_at                             timestamp with time zone
--   failed_at                                timestamp with time zone
--   failure_reason                           text
--   external_id                              text
--   metadata                                 jsonb
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY notification_delivery_log_pkey: (id)

-- ===== notification_recipients =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   notification_id                          uuid NOT NULL
--   user_id                                  uuid NOT NULL
--   status                                   text NOT NULL DEFAULT 'pending'::text
--   read_at                                  timestamp with time zone
--   acted_at                                 timestamp with time zone
--   dismissed_at                             timestamp with time zone
--   action_taken                             text
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY notification_recipients_pkey: (id)

-- ===== notifications =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   event_type                               text NOT NULL
--   title                                    text NOT NULL
--   body                                     text NOT NULL
--   payload                                  jsonb
--   resource_type                            text
--   resource_id                              uuid
--   priority                                 text NOT NULL DEFAULT 'normal'::text
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY notifications_pkey: (id)

-- ===== org_settings =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   setting_key                              text NOT NULL
--   setting_value                            jsonb NOT NULL
--   category                                 text NOT NULL DEFAULT 'general'::text
--   is_public                                boolean NOT NULL DEFAULT true
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE org_settings_org_id_setting_key_key: (org_id, setting_key)
--   PRIMARY KEY org_settings_pkey: (id)

-- ===== organizations =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   name                                     text NOT NULL
--   slug                                     text NOT NULL
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY organizations_pkey: (id)
--   UNIQUE organizations_slug_key: (slug)

-- ===== ownership_change_log =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   relationship_id                          uuid
--   from_entity_id                           uuid NOT NULL
--   to_entity_id                             uuid NOT NULL
--   prior_pct                                numeric
--   new_pct                                  numeric
--   change_reason                            text NOT NULL
--   change_source_type                       text
--   change_source_id                         uuid
--   effective_date                           date
--   note                                     text
--   changed_by                               uuid
--   changed_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY ownership_change_log_pkey: (id)

-- ===== permissions =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   name                                     text NOT NULL
--   resource                                 text NOT NULL
--   action                                   text NOT NULL
--   UNIQUE permissions_name_key: (name)
--   PRIMARY KEY permissions_pkey: (id)
--   UNIQUE permissions_resource_action_key: (resource, action)

-- ===== posting_template_lines =====
--   id                                       uuid NOT NULL DEFAULT gen_random_uuid()
--   template_id                              uuid NOT NULL
--   line_no                                  integer NOT NULL
--   account_code                             text NOT NULL
--   side                                     character NOT NULL
--   amount_source                            text NOT NULL DEFAULT 'event_amount'::text
--   dimension_source                         text NOT NULL DEFAULT 'none'::text
--   PRIMARY KEY posting_template_lines_pkey: (id)
--   UNIQUE ptl_line_unique: (template_id, line_no)

-- ===== posting_templates =====
--   id                                       uuid NOT NULL DEFAULT gen_random_uuid()
--   org_id                                   uuid NOT NULL
--   transaction_type_code                    text NOT NULL
--   name                                     text NOT NULL
--   vehicle_type_scope                       text NOT NULL DEFAULT 'any'::text
--   is_active                                boolean NOT NULL DEFAULT true
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY posting_templates_pkey: (id)

-- ===== profile_conversations =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   current_question_index                   integer NOT NULL DEFAULT 0
--   status                                   text NOT NULL DEFAULT 'in_progress'::text
--   messages                                 jsonb NOT NULL DEFAULT '[]'::jsonb
--   started_at                               timestamp with time zone NOT NULL DEFAULT now()
--   completed_at                             timestamp with time zone
--   created_by                               uuid
--   UNIQUE profile_conversations_entity_id_status_key: (entity_id, status)
--   PRIMARY KEY profile_conversations_pkey: (id)

-- ===== reference_data =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid
--   list_key                                 text NOT NULL
--   code                                     text NOT NULL
--   label                                    text NOT NULL
--   parent_code                              text
--   extra                                    jsonb
--   display_order                            integer NOT NULL DEFAULT 100
--   is_active                                boolean NOT NULL DEFAULT true
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE reference_data_list_key_code_parent_code_key: (list_key, code, parent_code)
--   PRIMARY KEY reference_data_pkey: (id)

-- ===== role_permissions =====
--   role_id                                  uuid NOT NULL
--   permission_id                            uuid NOT NULL
--   PRIMARY KEY role_permissions_pkey: (role_id, permission_id)

-- ===== roles =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   name                                     text NOT NULL
--   description                              text
--   UNIQUE roles_org_id_name_key: (org_id, name)
--   PRIMARY KEY roles_pkey: (id)

-- ===== spv_documents =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   spv_id                                   uuid NOT NULL
--   subscription_id                          uuid
--   doc_type                                 text NOT NULL
--   title                                    text NOT NULL
--   storage_key                              text
--   status                                   text NOT NULL DEFAULT 'draft'::text
--   uploaded_by                              uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY spv_documents_pkey: (id)

-- ===== spv_status_history =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   spv_id                                   uuid NOT NULL
--   from_status                              text
--   to_status                                text NOT NULL
--   note                                     text
--   changed_by                               uuid
--   changed_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY spv_status_history_pkey: (id)

-- ===== spv_subscriptions =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   spv_id                                   uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   member_investment_id                     uuid
--   commitment_amount                        numeric NOT NULL
--   funded_amount                            numeric NOT NULL DEFAULT 0
--   ownership_pct                            numeric
--   subscription_status                      text NOT NULL DEFAULT 'soft'::text
--   signed_at                                timestamp with time zone
--   valid_from                               timestamp with time zone NOT NULL DEFAULT now()
--   valid_to                                 timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY spv_subscriptions_pkey: (id)

-- ===== spv_transaction_allocations =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   transaction_id                           uuid NOT NULL
--   spv_id                                   uuid NOT NULL
--   subscription_id                          uuid NOT NULL
--   entity_id                                uuid NOT NULL
--   ownership_pct                            numeric NOT NULL
--   allocated_amount                         numeric NOT NULL
--   status                                   text NOT NULL DEFAULT 'allocated'::text
--   settled_at                               timestamp with time zone
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY spv_transaction_allocations_pkey: (id)

-- ===== spv_transactions =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   spv_id                                   uuid NOT NULL
--   txn_type                                 text NOT NULL
--   txn_date                                 date NOT NULL
--   amount                                   numeric NOT NULL
--   description                              text
--   reference                                text
--   allocation_basis                         text NOT NULL DEFAULT 'ownership_pct'::text
--   status                                   text NOT NULL DEFAULT 'draft'::text
--   allocated_at                             timestamp with time zone
--   posted_at                                timestamp with time zone
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   transaction_type_id                      uuid
--   currency_code                            text NOT NULL DEFAULT 'USD'::text
--   amount_basis                             text NOT NULL DEFAULT 'currency'::text
--   PRIMARY KEY spv_transactions_pkey: (id)

-- ===== spvs =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   deal_id                                  uuid NOT NULL
--   vehicle_entity_id                        uuid
--   name                                     text NOT NULL
--   spv_status                               text NOT NULL DEFAULT 'forming'::text
--   target_raise                             numeric
--   minimum_raise                            numeric
--   hard_cap                                 numeric
--   min_commitment                           numeric
--   currency                                 text NOT NULL DEFAULT 'USD'::text
--   carry_pct                                numeric
--   mgmt_fee_pct                             numeric
--   close_date                               date
--   formation_notes                          text
--   created_by                               uuid
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   vehicle_type                             text NOT NULL DEFAULT 'standalone_spv'::text
--   master_entity_id                         uuid
--   class_label                              text
--   UNIQUE spvs_deal_class_label_uniq: (deal_id, class_label)
--   PRIMARY KEY spvs_pkey: (id)

-- ===== transaction_types =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid
--   code                                     text NOT NULL
--   label                                    text NOT NULL
--   category                                 text NOT NULL
--   direction                                text NOT NULL
--   affects_paid_in                          integer NOT NULL DEFAULT 0
--   affects_unfunded                         integer NOT NULL DEFAULT 0
--   affects_nav                              integer NOT NULL DEFAULT 0
--   is_recallable                            boolean NOT NULL DEFAULT false
--   performance_impact                       text
--   applies_to_security_types                ARRAY
--   amount_basis                             text NOT NULL DEFAULT 'currency'::text
--   is_active                                boolean NOT NULL DEFAULT true
--   display_order                            integer NOT NULL DEFAULT 100
--   notes                                    text
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   UNIQUE transaction_types_code_key: (code)
--   PRIMARY KEY transaction_types_pkey: (id)

-- ===== user_notification_preferences =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   user_id                                  uuid NOT NULL
--   event_type                               text NOT NULL
--   channel                                  text NOT NULL
--   is_enabled                               boolean NOT NULL DEFAULT true
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   PRIMARY KEY user_notification_preferences_pkey: (id)
--   UNIQUE user_notification_preferences_user_id_event_type_channel_key: (user_id, event_type, channel)

-- ===== user_roles =====
--   user_id                                  uuid NOT NULL
--   role_id                                  uuid NOT NULL
--   PRIMARY KEY user_roles_pkey: (user_id, role_id)

-- ===== users =====
--   id                                       uuid NOT NULL DEFAULT uuid_generate_v4()
--   org_id                                   uuid NOT NULL
--   email                                    text NOT NULL
--   full_name                                text
--   avatar_url                               text
--   auth0_sub                                text
--   role                                     text NOT NULL DEFAULT 'member'::text
--   created_at                               timestamp with time zone NOT NULL DEFAULT now()
--   updated_at                               timestamp with time zone NOT NULL DEFAULT now()
--   assistant_panel_posture                  text
--   nav_pinned                               boolean NOT NULL DEFAULT false
--   UNIQUE users_auth0_sub_key: (auth0_sub)
--   UNIQUE users_email_key: (email)
--   PRIMARY KEY users_pkey: (id)

