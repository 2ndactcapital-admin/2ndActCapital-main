See the canonical version at /SPRINT_WORKFLOW_STANDARD.md (repo root). This
copy is deliberately a pointer, not a duplicate — two independently-editable
copies of "the single, canonical process" is the exact problem it exists to
prevent.
