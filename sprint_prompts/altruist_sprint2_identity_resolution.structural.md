ALTRUIST SPRINT 2 — Household & account identity resolution against portfolio.external_references. 4 tasks + verification.

Context: Sprint 1 (merged to main, commit e76c246) built altruist_connections/altruist_oauth_states with a working OAuth2 authorization-code flow, and a require_active_connection guard used by call_households(conn, org_id, environment) in apps/api/services/altruist_oauth.py — which currently raises NotImplementedError on purpose, once past the guard, because no real Altruist HTTP call had ever been made. This sprint replaces that NotImplementedError with a real implementation of GET /v2/households and GET /v2/accounts, and resolves identity between Altruist's IDs and Hollisworks' own uuids via the existing portfolio.external_references crosswalk table — not new tables. Per the Architecture Decisions in the companion spec, Hollisworks' own households/entities remain the source of truth; this sprint reads FROM Altruist to resolve/attach identity, it does not import Altruist's household/account structure wholesale.

CONFIRMED REAL FACTS, DO NOT RE-DERIVE:
- altruist_connections / altruist_oauth_states are live with org-isolation RLS proven both directions (Sprint 1 verify, 24/26 PASS). require_active_connection(conn, *, org_id, environment) returns the active connection row or raises AltruistNotConnected — reuse this guard, do not reimplement it.
- get_decrypted_refresh_token(conn, *, connection_id) and the module's encrypt_secret/decrypt_secret (Fernet, keyed by ALTRUIST_TOKEN_ENCRYPTION_KEY) already exist for reading/writing credential values — reuse them; do not add a second encryption path.
- No Altruist sandbox credentials exist in this project's Doppler config as of Sprint 1 (confirmed live via `doppler secrets --only-names`) — Sprint 0's outreach has not yet landed real access. This sprint's real HTTP call code should be written against the best-available documented shape, but CANNOT be exercised against a live Altruist response. Follow Sprint 1's own precedent (its Task 4 proved the OAuth token-exchange/refresh plumbing using a synthetic, documented-shape token response, never a live call) — this sprint's verify script must do the same for GET /v2/households and GET /v2/accounts: a synthetic response shaped per the Open API guide, with the exact field mapping flagged as a [FIND] to correct once real sandbox access exists, not silently assumed correct.
- Documented endpoints (re-confirm the exact paths live against /reference if reachable, rather than trust this table blindly — the households row's "/api/v2/households" vs the accounts row's "/v2/accounts" naming is inconsistent in the source Guides and may be a documentation quirk, not a real path difference): Households — GET /api/v2/households (list of household ids/names visible to the authorized user); Accounts — GET /v2/accounts?household_id=... (accounts for a household).
- org_id for 2nd Act Capital = 00000000-0000-0000-0000-000000000001. Hollisworks' own households live in public.households / public.household_memberships / entities.primary_household_id.
- Per the Architecture Decisions doc, portfolio.external_references (generic crosswalk) and public.account_import_exceptions already exist as tables — their exact live column shapes have NOT been independently verified this session and must be discovered fresh in Task 1, not assumed from the design doc's prose description.
- Standard RLS policy pattern for any table this sprint touches (should already exist on both tables above, but confirm): USING/WITH CHECK (org_id = (nullif(current_setting('app.current_org_id', true), ''))::uuid OR current_setting('app.is_super_admin', true) = 'true').

THERE IS NO HUMAN AVAILABLE. Report findings, then continue immediately in the same response. If uncertain, continue.

STANDING RULES: no interactive prompts; org_id never from a request body; credential values never logged in plaintext; this is application code only — no new tables, no migration file expected (Part 1 is deliberately empty; if Task 1 discovers the crosswalk tables' real shape can't actually support this without a schema change, STOP and report that as a [FIND] rather than improvising DDL).

=== TASK 1: DISCOVER ===
Report findings, THEN CONTINUE IMMEDIATELY in the same response.
  1a. Read the live column shape of portfolio.external_references and public.account_import_exceptions (information_schema, not the design doc's prose) — what columns exist, what's the natural key/unique constraint on external_references (must support org_id + external_system + external_type + external_id -> internal uuid), and what account_import_exceptions expects for a landed unmatched row.
  1b. Read apps/api/services/altruist_oauth.py's current call_households function and the module docstring's note on it, to confirm exactly what it guards and where the NotImplementedError sits, so this sprint extends rather than duplicates it.
  1c. Confirm the real shape of public.households / public.household_memberships / entities.primary_household_id (columns, how an entity's household is looked up) since this sprint needs to know, for a resolved Altruist account, which Hollisworks household/entity it should attach to when one already exists.
  1d. Confirm httpx is a real, installed dependency in this environment (Sprint 1 imported it but its own verify never exercised a real network call) and confirm requirements.txt actually declares it, not just cryptography.

=== TASK 2: BUILD ===
Based on Task 1's real findings:
  - Implement the real GET /v2/households and GET /v2/accounts calls in call_households (rename/extend as Task 1's discovery of the existing function suggests is cleanest — e.g. a real households fetch plus a new accounts-for-household fetch), using the active connection's decrypted access token, going through require_active_connection first exactly as before.
  - For each Altruist household/account: look up whether a matching row already exists in portfolio.external_references (org_id, external_system='ALTRUIST' or whatever Task 1's discovery confirms as the real convention, external_type='household'|'account', external_id=Altruist's id). If found, resolve to the existing Hollisworks uuid — do nothing else. If not found, do NOT auto-create a Hollisworks household/entity/account — insert a row into public.account_import_exceptions for manual review, per the Architecture Decisions' explicit "Hollisworks' own households/entities stay authoritative" rule.
  - Make the whole resolution pass idempotent: re-running it against the same Altruist data must not create duplicate external_references rows or duplicate account_import_exceptions rows for the same unmatched account.

=== TASK 3: SANDBOX SMOKE TEST ===
Confirm (again, live — do not assume Sprint 1's finding still holds) via `doppler secrets --only-names` that no ALTRUIST_* sandbox credentials exist yet. If still absent, as expected: report BLOCKED, do not attempt a live call, move immediately to Task 4 in the same response.

=== TASK 4: REAL PROOF (none of this needs live Altruist credentials) ===
  - Using a synthetic, documented-shape GET /v2/households + GET /v2/accounts response (monkeypatched transport, same technique as Sprint 1's token-exchange proof): a household/account that already has an external_references row resolves to the correct existing Hollisworks uuid, and does NOT create a duplicate row on a second run (idempotency, proven by literally re-running the same resolution pass twice and diffing row counts).
  - A synthetic household/account with NO existing external_references row lands exactly one row in account_import_exceptions, with enough information in it (Altruist id, household/account name, org_id) for a human reviewer to act on — and running the same pass again does not create a second exception row for the same unmatched account.
  - Prove RLS isolation both directions on whatever rows this sprint writes (external_references and account_import_exceptions): org A's session cannot read or write org B's rows, on the identical query with only the org GUC changed; org A's own rows are visible in the same test.
  - Reproduce the failure state first: calling the households/accounts resolution for an org with no active altruist_connections row still raises AltruistNotConnected before any network call — prove the guard from Sprint 1 is still being respected by this sprint's new code path, not bypassed.

=== TASK 5: UPDATE PROJECT STATUS ===
Write the verify script to apps/api/scripts/verify_altruist_sprint2_identity_resolution.py — the filename matters for the wrapper's own convention (must match this sprint's name, "altruist_sprint2_identity_resolution", exactly, before the tier suffix). Update docs/PROJECT_STATUS.md: identity resolution live, unmatched accounts route to account_import_exceptions, Sprint 3 (positions/transactions sync) unblocked for the parts that don't require a live call. Commit everything in one commit, matching Sprint 1's convention (e.g. `sprint: altruist_sprint2_identity_resolution.structural - identity resolution, N/N PASS + 1 BLOCKED (no sandbox credentials), HELD for manual review`).

=== VERIFICATION: apps/api/scripts/verify_altruist_sprint2_identity_resolution.py ===
Pass/fail only. No interactive prompts. One [Y]/[BLOCKED] assertion per Task 3/4 proof above, explicitly listed, plus a teardown check confirming every table this script writes to is back at its pre-test row count.
