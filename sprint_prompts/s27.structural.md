SPRINT 27 — TaskRouter: real model/task routing with a decision
log and a per-org fallback CHAIN. 3 tasks + verification. This
is core AI infrastructure — many existing features (assistant,
dashboard, investment_profile, marketplace, extraction, crm)
already call AI through some central mechanism from the
Mini-Bedrock sprint. Do NOT duplicate or bypass that mechanism —
extend it.

CONTEXT: ai_decision_log already exists (Part 1 SQL applied).
org_settings already has ai.model.default / ai.model.provider /
ai.model.fallback (a SINGLE fallback value, from Mini-Bedrock) —
this sprint upgrades fallback to a real ORDERED CHAIN, not just
one value, confirmed per-org (not platform-wide).

IMPORTANT DISTINCTION: "model version" does NOT need a separate
column — Anthropic's model strings are already dated/versioned
(e.g. claude-haiku-4-5-20251001) — logging the full model string
in ai_decision_log.model_used IS the version record.

STANDING RULES: org_id never from request body; Decimal for any
cost figures; no interactive prompts; light theme if UI touched.

=== TASK 1: Discover the real central AI-calling mechanism ===
Find the actual function(s) Mini-Bedrock created/touched for
resolving which model to call (referenced across
routers/assistant.py, dashboard.py, investment_profile.py,
marketplace.py, services/extraction.py,
services/assistant_actions/crm.py). Report:
  - The real function name(s)/file(s) — do not guess
  - Exactly how it currently resolves a model from org_settings
    (the real key names, the real fallback logic as it exists
    today — confirm ai.model.fallback is genuinely just one
    value, not already a list)
  - Every call site that uses it vs. any call site that still
    calls the Anthropic client directly, bypassing it (if any
    exist, flag them — Mini-Bedrock's own goal was zero
    bypasses, confirm that's still true)

=== TASK 2: Real ordered fallback chain, per org ===
  - Add a new org_settings key per task type (or extend the
    existing ai.model.* namespace sensibly, matching whatever
    real structure Task 1 found) storing an ORDERED JSON array
    of model strings — e.g. ai.model.fallback_chain — instead of
    the single-value ai.model.fallback. Seed the real default
    org's existing single fallback value as a one-item array
    (preserving current behavior exactly, not changing what
    actually gets called for existing functionality).
  - Update the central resolution function (found in Task 1) to
    walk this chain: try the primary model; on failure, log the
    failure and try the next model in the chain; continue until
    one succeeds or the chain is exhausted (raise clearly if so,
    do not silently return nothing).

=== TASK 3: Wire the decision log into every call ===
Every call through the central mechanism (found in Task 1) must
write a row to ai_decision_log: task_type (infer from context —
e.g. the calling feature/router), model_requested (the primary/
preferred model before any fallback), model_used (whichever
model actually responded), fallback_used + fallback_reason if a
fallback was needed, cost_usd, latency_ms, success, error_detail
if it ultimately failed. This must be genuinely non-blocking —
a logging failure must never break the actual AI call itself
(wrap logging in its own try/except, log-and-continue on error).

=== VERIFICATION ===
Write verify_s27.py (apps/api/scripts/) — pass/fail only, no
interactive prompts, idempotent, teardown-at-start and
teardown-at-end.

Assertions to include:
  [Y] ai_decision_log exists matching the snapshot
  [Y] Report Task 1's discovery findings explicitly (the real
      central function, confirm zero bypasses or list any found)
  [Y] A successful call through the central mechanism writes a
      correct row: model_requested == model_used,
      fallback_used = false, success = true, real cost/latency
      populated (not null/zero placeholders)
  [Y] Simulate a primary-model failure — confirm the chain
      correctly falls through to the next model in the org's
      fallback_chain, and the logged row shows
      model_requested != model_used, fallback_used = true, with
      a fallback_reason
  [Y] A DIFFERENT org with a DIFFERENT fallback_chain configured
      correctly uses ITS OWN chain, not another org's (proves
      per-org configuration actually works, not just seeded
      globally)
  [Y] An org with NO explicit fallback_chain set falls back to
      the existing DEFAULT_SETTINGS behavior (preserving
      Mini-Bedrock's existing default-org behavior exactly)
  [Y] A logging failure (simulate by breaking the log write)
      does NOT prevent the underlying AI call's actual result
      from being returned to the caller — logging is provably
      non-blocking
  [Y] Teardown: zero leftover rows, confirm via count(*)

Report each assertion explicitly. Push when 100% pass — hold for
manual review regardless of tier, since this touches the shared
AI-calling path used by multiple existing features.
