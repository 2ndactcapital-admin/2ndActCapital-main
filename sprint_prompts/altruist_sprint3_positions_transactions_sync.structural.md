ALTRUIST SPRINT 3 — Positions & transactions sync for resolved accounts. 4 tasks + verification.

Context: Sprint 1 (merged, e76c246) built the OAuth2 connection scaffold. Sprint 2 (merged, commit 77075e6 atop c294d93) built apps/api/services/altruist_identity.py, which resolves Altruist households/accounts against portfolio.external_references — a matched account gets an external_references row pointing at its Hollisworks account uuid; an unmatched account lands in public.account_import_exceptions for manual review instead. This sprint is the next step: for accounts that ARE already resolved (a real external_references row exists), pull that account's current positions and transaction history from Altruist and land them in Hollisworks' own position/transaction data model — using the 'ALT'/'ALT-DEF' custodian and custodian_system reference_data rows already seeded (list_key='custodian'/'custodian_system'). Unmatched accounts (still sitting in account_import_exceptions) are explicitly OUT of scope for this sprint — they must be skipped, not synced.

CONFIRMED REAL FACTS, DO NOT RE-DERIVE:
- require_active_connection / call_households (Sprint 1) and resolve_identity (Sprint 2, in apps/api/services/altruist_identity.py) are live and proven — reuse both; do not reimplement the guard or the resolution logic.
- portfolio.external_references is the crosswalk: for a given org_id + source + record_type + external_id, it holds the matched Hollisworks account uuid — query it directly to enumerate which accounts are actually in scope for this sprint (matched only).
- reference_data (list_key='custodian', code='ALT') and (list_key='custodian_system', code='ALT-DEF') already exist, seeded and live — use these codes when tagging Altruist-sourced positions/transactions, do not invent new custodian codes.
- No Altruist sandbox credentials exist in Doppler as of Sprint 2 (re-confirm live, do not assume it still holds) — this sprint's real HTTP call code must be proven via a synthetic, documented-shape response, exactly like Sprints 1 and 2's own precedent, never a live call.
- The exact documented paths for a positions endpoint and a transactions endpoint have NOT been independently confirmed this session (unlike households/accounts, which Sprint 1/2 confirmed against the Guides). Task 1 must actively try to confirm them — check this repo for any existing Altruist API reference doc first (e.g. anything under docs/ mentioning altruist positions/transactions); if genuinely not found and not reachable, fall back to the established /v2/<resource> convention (e.g. GET /v2/positions?account_id=..., GET /v2/transactions?account_id=...) as a best-guess default, and flag the path itself as an explicit unconfirmed [FIND] to correct once real docs or sandbox access exist — do not let path uncertainty block the sprint, same as Sprint 1/2's precedent for synthetic-response proofs.

THERE IS NO HUMAN AVAILABLE. Report findings, then continue immediately in the same response. If uncertain, continue.

STANDING RULES: no interactive prompts; org_id never from a request body; only accounts with a real external_references match are ever synced — an account still sitting in account_import_exceptions must be skipped, proven by test, not just assumed by construction; money/quantity values are Decimal, never float; this repo's ledger entries are immutable (amend/reverse, never overwrite) — if positions/transactions need to land in or alongside the existing general-ledger/portfolio schema, respect that immutability discipline exactly, do not bypass it for convenience. IMPORTANT — schema caution: if Task 1 discovers there is NO existing table capable of holding custodian-sourced positions/transactions (i.e. this is genuinely new schema, not an extension of something that already exists), STOP after Task 1, do not improvise financial-ledger-adjacent DDL unsupervised — report exactly what's missing and what shape would be needed as a [FIND], and end the response there. Only proceed to Task 2 if Task 1 confirms a real, already-existing table (or tables) this sprint can extend.

=== TASK 1: DISCOVER ===
Report findings, THEN CONTINUE IMMEDIATELY in the same response (unless the STOP condition above applies).
  1a. Find the live table(s) that already hold custodian-sourced positions and transaction/activity data (search information_schema for plausible names under portfolio.* and public.* — e.g. anything with "position", "holding", "transaction", "activity" in the name — do not assume a name). Confirm their real columns: does a row carry a custodian/custodian_system reference, an account reference, a security/instrument identifier, quantity, price, amount, as-of date, bitemporal columns? Is there already a precedent for how a DIFFERENT custodian's data lands in these tables (even if via a batch/flat-file process rather than a live API) that this sprint should conform to?
  1b. Read apps/api/services/altruist_identity.py's current state in full to confirm exactly how it queries portfolio.external_references for a resolved account, so this sprint reuses that exact query shape rather than re-deriving it.
  1c. Check this repo (docs/, any Altruist-related markdown) for an existing reference to positions/transactions endpoint paths; if none exists, note that explicitly and use the best-guess default described above.
  1d. Confirm the real shape of any existing security/instrument reference table (e.g. how a ticker/CUSIP is represented elsewhere in the schema already), since a synced position needs to reference or create the correct instrument identity — do not invent a second, parallel instrument model if one already exists.

=== TASK 2: BUILD ===
Based on Task 1's real findings (and only if the STOP condition did not apply):
  - Implement real GET calls for positions and transactions for a resolved account, using the active connection's decrypted access token via require_active_connection, going through altruist_identity.py's existing account-resolution query to enumerate in-scope (matched) accounts only.
  - Write positions/transactions into the real, already-existing table(s) Task 1 identified, tagged with the ALT/ALT-DEF custodian/custodian_system codes, respecting whatever bitemporal/immutability convention that table already follows.
  - Make the sync idempotent and safe to re-run: re-syncing the same account's same data must not create duplicate rows — either upsert on a real natural key, or (if the existing table's convention is append-only/immutable) correctly supersede the prior row rather than blindly re-insert, matching however Task 1 found the existing precedent works.
  - Explicitly skip any account still in account_import_exceptions (unresolved) — do not sync it.

=== TASK 3: SANDBOX SMOKE TEST ===
Confirm live (do not assume Sprint 2's finding still holds) via `doppler secrets --only-names` that no ALTRUIST_* sandbox credentials exist yet. If still absent: report BLOCKED, do not attempt a live call, move immediately to Task 4 in the same response.

=== TASK 4: REAL PROOF (none of this needs live Altruist credentials) ===
  - Using a synthetic, documented-or-best-guess-shape GET positions/transactions response (monkeypatched transport, same technique as Sprints 1-2): a resolved account's positions/transactions land correctly in the real table(s), tagged with the correct custodian/custodian_system codes and pointing at the correct Hollisworks account uuid.
  - Re-running the same sync twice produces IDENTICAL row counts / state the second time — proven by literally running it twice and diffing, not asserted.
  - An account still sitting in account_import_exceptions (unresolved) is proven to be skipped — run the sync across a mixed set of resolved + unresolved accounts and confirm only the resolved one produced rows.
  - Reproduce the failure state first: calling the sync for an org with no active altruist_connections row still raises AltruistNotConnected before any network call.
  - Prove RLS isolation both directions on whatever table(s) this sprint writes to: org A's session cannot read or write org B's synced rows, on the identical query with only the org GUC changed; org A's own rows are visible in the same test.

=== TASK 5: UPDATE PROJECT STATUS ===
Write the verify script to apps/api/scripts/verify_altruist_sprint3_positions_transactions_sync.py — filename must match this sprint's name, "altruist_sprint3_positions_transactions_sync", exactly, before the tier suffix. Update docs/PROJECT_STATUS.md: positions/transactions sync live for resolved accounts, unresolved accounts correctly excluded, note the unconfirmed endpoint-path [FIND] prominently if it applies. Commit everything in one commit, matching the established convention (e.g. `sprint: altruist_sprint3_positions_transactions_sync.structural - positions/transactions sync, N/N PASS + 1 BLOCKED (no sandbox credentials), HELD for manual review`).

=== VERIFICATION: apps/api/scripts/verify_altruist_sprint3_positions_transactions_sync.py ===
Pass/fail only. No interactive prompts. One [Y]/[BLOCKED] assertion per Task 3/4 proof above, explicitly listed, plus a teardown check confirming every table this script writes to is back at its pre-test row count.
